# Web / API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Config & validation
pydantic==2.10.4
pydantic-settings==2.7.1

# Database (psycopg 3, sync, with connection pool)
psycopg[binary,pool]==3.2.3

# Queue / workers
celery[redis]==5.4.0
redis==5.2.1

# Auth — verify Supabase-issued JWTs
pyjwt==2.10.1

# Crypto — encrypt OAuth tokens at rest
cryptography==44.0.0

# Resilience — retries/backoff around external calls
tenacity==9.0.0

# HTTP client
httpx==0.28.1

# LLM provider (Groq free tier) + tracing
groq==0.13.1
langfuse==2.57.0

# Structured logging
structlog==24.4.0

# Mailbox ingestion — HTML email body extraction
beautifulsoup4==4.12.3

# Knowledge base — document text extraction (PDF / DOCX)
pypdf==5.1.0
python-docx==1.1.2

# Tests
pytest==8.3.4
